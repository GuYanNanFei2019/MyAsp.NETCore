@model List<RolesInUserViewModel>;
@{
	ViewBag.Title = "管理用户的角色";

	var userId = ViewBag.userId;
}

<form method="post">
	<input hidden asp-for="@userId"/>
	<div class="card">
		<div class="card-header">
			<h3>管理用户的角色</h3>
		</div>
		<div class="card-body">
			@*@foreach (var item in Model)
		{
		<div class="form-check m-1">
			<input hidden asp-for="@item.RoleId" />
			<input hidden asp-for="@item.RoleName" />
			<input asp-for="@item.IsSelected" class="form-check-input"/>
			<label class="form-check-label" asp-for="@item.IsSelected">
				@item.RoleName
			</label>
		</div>
		}*@

			@for (int i = 0; i < Model.Count; i++)
			{
				<div class="form-check m-1">
					<input hidden asp-for="@Model[i].RoleId" />
					<input hidden asp-for="@Model[i].RoleName" />
					<input asp-for="@Model[i].IsSelected" class="form-check-input" />
					<label class="form-check-label" asp-for="@Model[i].IsSelected">
						@Model[i].RoleName
					</label>
				</div>
			}

			<div class="text-danger" asp-validation-summary="All"></div>
		</div>

		<div class="card-footer">
			<input type="submit" class="btn btn-outline-success" value="更新"/>
			<a asp-controller="Admin" asp-action="EditUser" asp-route-Id="@userId" class="btn btn-outline-primary">取消</a>
		</div>
	</div>
</form>